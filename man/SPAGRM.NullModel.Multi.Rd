% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GRAB_SPAsqr.R
\name{SPAGRM.NullModel.Multi}
\alias{SPAGRM.NullModel.Multi}
\title{Calculate joint genotype distributions for families with outliers}
\usage{
SPAGRM.NullModel.Multi(subjData, ResidMat, SparseGRM, PairwiseIBD, control)
}
\arguments{
\item{subjData}{character vector. Subject IDs.}

\item{ResidMat}{numeric matrix. Residuals matrix (rows = subjects, columns = taus).}

\item{SparseGRM}{data.table. Sparse genetic relationship matrix with columns ID1, ID2, Value.}

\item{PairwiseIBD}{data.table. Pairwise IBD coefficients with columns ID1, ID2, pa, pb, pc.}

\item{control}{list. Control parameters including taus, MaxQuantile, MinQuantile, OutlierRatio,
ControlOutlier, MaxNuminFam, MAF_interval.}
}
\value{
A list with the following components:
\item{Resid}{numeric matrix. Residuals matrix used in analysis (N × ntaus).}
\item{subjData}{character vector. Subject IDs (length = N).}
\item{N}{integer. Number of subjects.}
\item{Resid.unrelated.outliers_lst}{list. Residuals of unrelated outlier subjects for each tau.}
\item{R_GRM_R_vec}{numeric vector. Sum of quadratic form Resid' * GRM * Resid for all subjects (length = ntaus).}
\item{R_GRM_R_TwoSubjOutlier_vec}{numeric vector. Aggregate contribution from two-subject outlier families (length = ntaus).}
\item{sum_R_nonOutlier_vec}{numeric vector. Sum of residuals for non-outlier unrelated subjects (length = ntaus).}
\item{R_GRM_R_nonOutlier_vec}{numeric vector. Quadratic form contribution for non-outlier unrelated subjects (length = ntaus).}
\item{TwoSubj_list}{list. Each element contains \code{Resid} (matrix, 2 × ntaus) and \code{Rho} (numeric vector, length 2) for two-member families.}
\item{ThreeSubj_list}{list. Each element contains \code{CLT} (Chow-Liu tree structure) and \code{stand.S} (matrix, 3^n × ntaus) for larger families.}
\item{MAF_interval}{numeric vector. MAF breakpoints used in tree construction.}
}
\description{
This function identifies outliers across multiple quantiles, decomposes family
structures using graph theory, and builds Chow-Liu trees for approximating
joint genotype distributions in related samples.
}
\keyword{internal}
