% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LEAF.R
\name{LEAF.Cluster}
\alias{LEAF.Cluster}
\title{Perform K-means Clustering and Ancestry-Matched Reference AF Synthesis}
\usage{
LEAF.Cluster(
  GenoFile = NULL,
  GenoFileIndex = NULL,
  Geno.mtx = NULL,
  control = list(AlleleOrder = "ref-first"),
  Ncluster = 1,
  subjData,
  PCmatrix,
  RefAfFile
)
}
\arguments{
\item{GenoFile}{Path to genotype file (PLINK/BGEN)}

\item{GenoFileIndex}{Optional genotype file index}

\item{Geno.mtx}{Optional genotype matrix (alternative to GenoFile)}

\item{control}{Control parameters list (e.g., AlleleOrder)}

\item{Ncluster}{Number of ancestry clusters (typically 2-5)}

\item{subjData}{Character vector of subject IDs}

\item{PCmatrix}{Matrix or data.frame of principal components for clustering (recommend first 4 PCs)}

\item{RefAfFile}{Path to reference allele frequency file with population-specific AF/AN columns}
}
\value{
List with two components:
\item{clusterIdx}{Integer vector of cluster assignments for each subject}
\item{subGenoInfo_lst}{List of data.frames (one per cluster) with ancestry-matched AF_ref and AN_ref}
}
\description{
Uses k-means clustering on principal components to partition samples into
ancestry-informed clusters. For each cluster, estimates ancestry proportions
using Summix and synthesizes ancestry-matched reference allele frequencies
and sample sizes from multi-population reference data.
}
\details{
Column naming in RefAfFile is flexible: supports both underscore ("_") and dot (".")
separators, case-insensitive matching (e.g., EUR_AF/eur.af both work).
Ancestry-matched AF_ref is computed as weighted sum: Sum(Proportion_i * AF_i).
Effective sample size AN_ref is computed as: 1 / Sum(Proportion_i^2 / AN_i).
}
\keyword{internal}
